package np.ict.mad.madassg2025

import androidx.compose.runtime.Composable

/**
 * Placeholder file so the project structure is ready.
 * Next step: we’ll implement a real Apple-like Sun Path arc here.
 */
@Composable
fun SunPathCard(
    sunriseUtc: Long,
    sunsetUtc: Long,
    tzOffsetSec: Int,
    nowUtcSec: Long
) {
    // Intentionally empty for now (we’ll fill this next).
}
